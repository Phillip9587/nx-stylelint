{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "NxStylelintScssConfiguration",
  "title": "Add scss support to a nx-stylelint configuration.",
  "type": "object",
  "x-deprecated": "This generator is deprecated and will be removed in v17. Use the configuration generator with the scss option instead.",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "skipFormat": {
      "description": "Skip formatting files.",
      "type": "boolean",
      "default": false
    }
  },
  "additionalItems": false,
  "required": ["project"]
}
